# festivals images

Place photos for the "festivals" gallery category here (JPG, PNG or WebP).

Recommended: compress images before upload (WebP, under ~300KB each)
so the site stays fast. Once you add real files here, update the
matching tiles in gallery.html (or, if you rebuild with build.py,
add <img> tags pointing to assets/images/festivals/your-photo.webp).
